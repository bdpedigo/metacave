# metacave

To add a repo: 

```bash
git submodule add --depth 1 https://github.com/seung-lab/zmesh.git submodules/zmesh
```

Then point the `add-repo` skill at the new repo to add it to the stack map.